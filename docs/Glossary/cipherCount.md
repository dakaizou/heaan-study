`cipherCount` is the number of `Ciphertext`. calculated as:

> $ceil(featureCount / batch)$

So generally, it can be seen as `how many features are there in one batch`.

It was originally called `cnum`.
